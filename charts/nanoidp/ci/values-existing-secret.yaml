# configFiles.existingSecret skips the generated Secret entirely; the
# named Secret is expected to already exist in the cluster (helm
# lint/template render fine either way, they don't validate that it does).
configFiles:
  existingSecret: my-existing-nanoidp-config
